<h1>Current user watchlists </h1>
<table>
  <tr>
    <th>Movie Name</th>
    <th></th>
  </tr>

  <% @watchlists.each do |watchlist| %>
    <tr>
      <td><%= watchlist.title %></td>
      <td><%= link_to 'Delete', watchlist_path(watchlist),
             method: :delete,
              data: { confirm: 'Are you sure' } %></td>
    </tr>

  <% end %>

</table>
<%= link_to 'Back', movies_path %> 

